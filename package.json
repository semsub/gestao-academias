{
  "name": "gestao-academias",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:build:debug": "cd capacitor/android && ./gradlew assembleDebug",
    "cap:build:release": "cd capacitor/android && ./gradlew assembleRelease",
    "cap:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
